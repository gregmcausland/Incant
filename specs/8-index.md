# Task: Implement index.ts

## Goal
Expose the clean public API for the Incant library.

## File
`src/index.ts`

## Responsibilities
- Export `createAgent`, `runAgent` from core
- Export `GeminiWrapper` from llm
- Export `tool` from tools
- Export `createBufferMemory` from memory
